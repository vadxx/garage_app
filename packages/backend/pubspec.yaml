name: backend
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: ^3.12.0

dependencies:
  freezed_annotation: ^2.4.1
  sqlite3: ^3.3.2

dev_dependencies:
  build_runner: ^2.4.12
  freezed: ^2.5.7
  lints: ^6.0.0
  test: ^1.25.6
